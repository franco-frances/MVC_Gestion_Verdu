@model MVC_GestionVerdu.Models.Register


@{
    ViewData["Title"] = "Registro";
}

<div class="container d-flex justify-content-center align-items-center vh-100">
    <div class="card shadow p-4" style="width: 350px;">
        <h3 class="text-center mb-3">Registro</h3>

        <!-- Mostrar errores generales -->
        @if (!ViewData.ModelState.IsValid)

        {
            <div class="alert alert-danger">
                <ul>
                    @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                    {
                        <li>@error.ErrorMessage</li>
                    }
                </ul>
            </div>
        }

        <form method="post" action="@Url.Action("Register", "Auth")">
            <div class="mb-3">
                <label asp-for="NickName" class="form-label">Nombre de usuario</label>
                <input asp-for="NickName" type="text" class="form-control" id="nickname" value="@Model?.NickName" required>
                <span asp-validation-for="NickName" class="invalid-feedback"></span>

            </div>
            <div class="mb-3">
                <label asp-for="Email" class="form-label">Correo Electrónico</label>
                <input asp-for="Email" type="email" class="form-control" id="email"  value="@Model?.Email" required>
                <span asp-validation-for="Email" class="invalid-feedback"></span>

            </div>
            <div class="mb-3">
                <label asp-for="Password" class="form-label">Contraseña</label>
                <input asp-for="Password" type="password" class="form-control" id="password"  required>
                <span asp-validation-for="Password" class="invalid-feedback"></span>

            </div>
            <div class="mb-3">
                <label asp-for="ConfirmPassword" class="form-label">Confirmar Contraseña</label>
                <input asp-for="ConfirmPassword" type="password" class="form-control" id="confirmPassword" name="confirmPassword" required>
                <span asp-validation-for="ConfirmPassword" class="invalid-feedback"></span>

            </div>
            <button type="submit" class="btn btn-success w-100">Registrarse</button>
        </form>

        <div class="mt-3 text-center">
            <p>¿Ya tienes cuenta? <a href="@Url.Action("Login", "Auth")">Inicia sesión aquí</a></p>
        </div>
    </div>
</div>
